<!DOCTYPE html>
<html>
  <head>
    <title>EPEsi</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render "shared/header" %>
    <main style="min-height: calc(100vh - 104.44px); display: flex; align-items: center; flex-direction: column">
    <%= yield %>
    </main>
    <%= render "shared/footer" %>
  </body>
</html>
