<%= form_with(model: user_area) do |form| %>
  <% if user_area.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_area.errors.count, "error") %> prohibited this user_area from being saved:</h2>

      <ul>
        <% user_area.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
